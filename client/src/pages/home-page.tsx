import React from "react";
import { Box } from "@mui/material";

const HomePage = () => (
  <Box
    sx={{
      display: "flex",
      flexDirection: "row",
      alignItems: "flex-start",
    }}
  />
);

export default HomePage;
